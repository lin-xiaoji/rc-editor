import React from 'react';
const Split = (props) => {
    return (
        <div className={`${props.prefixCls}-toolbar-split`}>|</div>
    )
};

export default {
    name: '|',
    component: Split
};
